name: borneo_kernel_abstractions
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
resolution: workspace
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.0

# Add regular dependencies here.
dependencies:
  borneo_common:
    path: ../borneo_common
  cancellation_token: ^2.0.1
  event_bus: ^2.0.1
  pub_semver: ^2.2.0
  synchronized: ^3.4.0
  logger: ^2.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.26.0
